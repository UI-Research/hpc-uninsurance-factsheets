---
header-includes:
- \input{preamble.tex}
fontsize: 10pt
output: pdf_document
sansfont: Lato
font:     Lato
geometry: "left=1in,right=1in,top=0.35in,bottom=0.6in"
urlcolor: #169d62
params:
  parameter1: NA
---
\raggedright
\normalsize
```{r rmarkdown-setup, include=FALSE}
knitr::opts_chunk$set(echo = FALSE)
knitr::opts_chunk$set(message = FALSE)
knitr::opts_chunk$set(warning = FALSE)
knitr::opts_chunk$set(dev = "cairo_pdf")
options(knitr.kable.NA = "")
options(kableExtra.latex.load_packages = FALSE)

set_urbn_defaults(style = "print")

state_table <- table1 %>%
  filter(statename == params$parameter1)
country_table <- table1 %>%
  filter(statename == "US") 

is_small <- state_table$small
is_large <- !(is_small)

# apply formats to state_table
state_table[,grep("_mn",colnames(state_table))]<-state_table[,grep("_mn",colnames(state_table))]*100
state_table[,grep("_share",colnames(state_table))]<-state_table[,grep("_share",colnames(state_table))]*100
state_table[,grep("_benefit",colnames(state_table))]<-state_table[,grep("_benefit",colnames(state_table))]*100

# apply formats to country_table
country_table[,grep("_mn",colnames(country_table))]<- country_table[,grep("_mn",colnames(country_table))]*100

#gather state data ending in _mn
state_mn_data <- state_table %>% 
  select(ends_with("_mn")) %>% 
  gather(key="var",value="mn_value")
#join state _tstat data with _mn data 
state_data_with_tstats <- state_table %>% 
  select(ends_with("_tstat")) %>% 
  gather(key="var",value="tstat_value") %>%
  mutate(var = paste(str_sub(var, 1, str_length(var)-6), "_mn", sep="")) %>%
  left_join(state_mn_data, by = c("var"="var"))
#join state _n data with _mn and _tstat data 
state_data_with_n <- state_table %>% 
  select(ends_with("_n")) %>% 
  gather(key="var",value="n_value") %>%
  mutate(var = paste(str_sub(var, 1, str_length(var)-2), "_mn", sep="")) %>%
  left_join(state_data_with_tstats, by = c("var"="var"))
#combine all data for state, join pie labels and language for text
total_state_data_with_labels <- datdict %>%
  select(Variable.Name, Label.for.Figures, Language.for.Text) %>%
  right_join(state_data_with_n, by = c("Variable.Name"="var")) 
total_state_data_with_labels$location <- toString(state_table$statename)

#gather country data ending in _mn
country_mn_data <- country_table %>% 
  select(ends_with("_mn")) %>% 
  gather(key="var",value="mn_value")
#join country _tstat data with _mn data 
country_data_with_tstats <- country_table %>% 
  select(ends_with("_tstat")) %>% 
  gather(key="var",value="tstat_value") %>%
  mutate(var = paste(str_sub(var, 1, str_length(var)-6), "_mn", sep="")) %>%
  left_join(country_mn_data, by = c("var"="var"))
#join country _n data with _mn and _tstat data 
country_data_with_n <- country_table %>% 
  select(ends_with("_n")) %>% 
  gather(key="var",value="n_value") %>%
  mutate(var = paste(str_sub(var, 1, str_length(var)-2), "_mn", sep="")) %>%
  left_join(country_data_with_tstats, by = c("var"="var"))
#combine all data for country, join pie labels and language for text
total_country_data_with_labels <- datdict %>%
  select(Variable.Name, Label.for.Figures, Language.for.Text) %>%
  right_join(country_data_with_n, by = c("Variable.Name"="var")) 
total_country_data_with_labels$location <- 'US'

all_data <- rbind(total_country_data_with_labels, total_state_data_with_labels) %>%
  filter(Variable.Name != "hier_mn")

if(state_table$expansion == 0){
  if (state_table$note == 0) {
    statement1 <- "which did not expand Medicaid under the Affordable Care Act"
  } else if (state_table$note == 1) {
    statement1 <- "which expanded Medicaid under the Affordable Care Act after 2017" 
  } else if (state_table$note == 2) {
    statement1 <- "which has voted to expand but not yet implemented Medicaid expansion under the Affordable Care Act" 
  }
} else{
    statement1 <- "which expanded Medicaid under the Affordable Care Act" 
}

if( state_table$ui_tstat<1.960 | state_table$ui_mn==country_table$ui_mn) {
statement2 <- "comparable"
statement3 <- "to" 
} else if ( state_table$ui_tstat>1.960 & state_table$ui_mn > country_table$ui_mn) {
statement2 <- "higher"
statement3 <- "than"
} else if ( state_table$ui_tstat>1.960 & state_table$ui_mn < country_table$ui_mn) {
statement2 <- "lower"
statement3 <- "than"
} 

statement4_frame <- total_state_data_with_labels %>% 
  filter(mn_value > state_table$ui_mn) %>% 
  filter(tstat_value>=1.960 ) %>%
  filter(n_value>=200) %>%
  select(Language.for.Text, mn_value, tstat_value)
statement4_list <- statement4_frame[["Language.for.Text"]] 
start <- paste(statement4_list[1:length(statement4_list)-1], collapse = ", ")
end <- statement4_list[length(statement4_list)]
if(length(statement4_list) == 1){
  statement4 <- paste0(" ", end)
  statement9 <- "had a higher uninsurance rate than the state average"
} else if (length(statement4_list) == 2){
  statement4 <- paste0(" ", start, " and ", end)
  statement9 <- "had higher uninsurance rates than the state average"
} else {
  statement4 <- paste0(" ", start, ", and ", end)
  statement9 <- "had higher uninsurance rates than the state average"
}

if (state_table$expansion == 1 | state_table$note == 1){
  statement5 <- "further expanding access to Medicaid or other highly subsidized insurance, as well as targeted outreach and enrollment efforts to subgroups of already eligible women with the highest uninsurance rates."
} else{
  statement5 <- "expanding access to Medicaid or other highly subsidized insurance, as well as targeted outreach and enrollment efforts to subgroups of already eligible women with the highest uninsurance rates."
}

if(state_table$ui_n >= 1000000){
  statement7 <- paste(round(state_table$ui_n/1000000, 1), "million", " ")
} else{
  statement7 <- statement7 <- prettyNum(round(state_table$ui_n, -3),big.mark=",",scientific=FALSE)
}

if(state_table$statename == "the District of Columbia"){
  statement8 <- "DC"
} else{
  statement8 <- state_table$statename
}

# Pie chart caption options
pie_caption1<-ifelse(state_table$fpwaiver == 1, "Some uninsured women in all categories may be eligible for or enrolled in a Medcaid plan that covers family planning services only. ","")
pie_caption2<-ifelse(state_table$note == 5|state_table$note == 4, "Some women likely eligible for Marketplace premium assistance may be eligible for more affordable coverage through a state-specific program. ","")

if (state_table$expansion == 1) {
    # expansion states looking ahead text
    lookingahead<-paste("Following Medicaid expansion in ",state_table$statename, ", the uninsurance rate fell from ", format(round(state_table$ui_mn13, 1), nsmall=1) ," percent in 2013 to ", format(round(state_table$ui_mn, 1), nsmall=1), " percent in 2017. Despite coverage gains, approximately ", statement7, " ", state_table$statename," women of reproductive age remained uninsured in 2017.",sep='')
} else {
  # nonexpansion states beginning of looking ahead text
  if (state_table$expansion == 0 & state_table$note == 1) {
    lookingahead_start<-"The recent Medicaid expansion in "
  } else if (state_table$expansion == 0 & state_table$note == 2) {
    lookingahead_start<-"The recent vote to expand Medicaid in "
  } else if (state_table$expansion == 0) {
    lookingahead_start<-"Expanding Medicaid in "
  } 
  # nonexpansion states full xlooking ahead text
  if (state_table$expansion == 0 & state_table$fpwaiver == 1 & state_table$hier_fpw_share>0) {
    lookingahead<-paste(lookingahead_start,state_table$statename," could benefit approximately ", format(round(state_table$hier_benefit, 1),nsmall=1), " percent of uninsured ", state_table$statename, " women of reproductive age, including currently ineligible citizens with incomes below the FPL (",format(round(state_table$hier_fpw_share, 1), nsmall=1), " percent) and those with incomes between 100 and 138 percent of the FPL (",format(round(state_table$hier_100_138_share, 1), nsmall=1)," percent) for whom Medicaid may be more affordable than Marketplace coverage.",sep='')
  } else if (state_table$expansion == 0 & state_table$fpwaiver == 1 & state_table$hier_fpw_share==0) {
    lookingahead<-paste(lookingahead_start,state_table$statename," could benefit approximately ", format(round(state_table$hier_benefit, 1),nsmall=1), " percent of uninsured ", state_table$statename, " women of reproductive age, including those with incomes between 100 and 138 percent of the FPL (",format(round(state_table$hier_100_138_share, 1), nsmall=1)," percent) for whom Medicaid may be more affordable than Marketplace coverage.",sep='')
  }else if (state_table$expansion == 0 & state_table$fpwaiver == 0) {
    lookingahead<-paste(lookingahead_start,state_table$statename," could benefit approximately ", format(round(state_table$hier_benefit, 1),nsmall=1), " percent of uninsured ", state_table$statename, " women of reproductive age, including currently ineligible citizens with incomes below the FPL (",format(round(state_table$hier_pov_none_share, 1), nsmall=1), " percent) and those with incomes between 100 and 138 percent of the FPL (",format(round(state_table$hier_100_138_share, 1), nsmall=1)," percent) for whom Medicaid may be more affordable than Marketplace coverage.",sep='')
  }
}

outreach<-paste0("Outreach and enrollment efforts targeted at subgroups of women with high uninsurance rates and those already eligible for assistance could help reduce the uninsurance rate in ", state_table$statename, ", though higher subsidies may be required to help address affordability barriers. In addition to continuing to monitor the uninsurance rate, it will be critical to track women’s ability to access the general and reproductive health services they need. This will include monitoring the availability and capacity of providers that disproportionately serve low-income and uninsured women, such as community health centers and Title X clinics.")

authors <-("This fact sheet was prepared by Stacey McMorrow, Emily M. Johnston, and Tyler W. Thomas, with R programming support from Leah Durbak and Michelle Menezes. For information on data and methods, along with national estimates of health insurance coverage among women of reproductive age in 2017, download the technical appendix at \\url{https://www.urban.org/uninsurance-among-reproductive-age-women-2017}.")
```
```{r echo=FALSE}
#Add blank data for spaces in chart
Label.for.Figures = c('space1', 'space2', 'space3','space4','space5','space6')
blank_data_for_chart <- data.frame(Label.for.Figures)
blank_data_for_chart$mn_value = 0
blank_data_for_chart$location = 'US'
blank_data_for_chart$n_value = 0
#Obtain data rows that have sample size below 200 sample size
state_data_below_200_n <- all_data %>%
  filter(n_value<200 | is.na(n_value)) 
#If there are rows with sample size < 200, set statement 6
if(nrow(state_data_below_200_n) > 0){
  statement6 <- " Subgroups with no state estimate had a sample size smaller than 200."
} else{
  statement6 <- ""
}

#overflow for second bullet
overflow_bullet <- c("Alaska", "Arkansas", "Colorado", "Idaho", "Indiana", "Iowa", "Louisiana", "New Hampshire", "the District of Columbia", "West Virginia")

#overflow for second bullet in alternate measures
#overflow_bullet <- c("Arizona", "Arkansas", "Colorado", "Idaho", "Indiana", "Iowa", "Kansas", "Louisiana")

if(params$parameter1 %in% overflow_bullet){
  statement10 <- "in \\linebreak 2017."
} else {
  statement10 <- "in 2017."
}

#Obtain columns needed for chart, set all mn_value with sample size<200 to 0, bind blank data rows  
total_data_for_chart <- all_data %>%
  select(Label.for.Figures, mn_value, n_value, location) %>%
  mutate(mn_value=ifelse(Label.for.Figures %in% state_data_below_200_n$Label.for.Figures & location==state_table$statename,0,mn_value)) %>%
    mutate(location=ifelse(location=="the District of Columbia","DC",location)) %>%
  rbind(blank_data_for_chart)
#Factor order for subgroups on plot x axis
plot_order <- c("Not in labor force", "Unemployed", "Employed", "space6","Not in metropolitan area", "In metropolitan area", "space5","Above 400% FPL", "251%–400% FPL", "139%–250% FPL", "0%–138% FPL", "space4", "Single, childless", "Married, childless", "Single mother", "Married mother", "space3", "Other/multiple races", "Hispanic", "Black", "White", "space2", "Ages 35–44", "Ages 26–34", "Ages 19–25", "Ages 15–18", "space1", "All women (ages 15–44)")
state_for_chart <- ifelse(state_table$statename=="the District of Columbia","DC",as.character(state_table$statename))
#Factor order for location on plot x axis
state_order <- c("US", state_for_chart)
#Generate bar chart
state_plot <- total_data_for_chart %>%
  mutate(location = factor(as.character(location), levels = state_order)) %>%
  mutate(Label.for.Figures = factor(Label.for.Figures, levels = plot_order)) %>%
  ggplot(aes(x=Label.for.Figures, y=mn_value, fill=location)) +
  geom_col(position=position_dodge()) +
  geom_text(aes(x = Label.for.Figures, y = mn_value, label=ifelse(location!="US" & mn_value>0,paste0(format(mn_value, digits=1, nsmall=1),"%"),'')), position = position_dodge(width = 1), hjust = -.25) +
  theme(
  axis.text.y=element_text(size=8.5),
  plot.title = element_blank(),
  axis.title.x = element_blank(),
  axis.title.y = element_blank(),
  axis.line.x=element_blank(),
  axis.line.y = element_line(colour = "black"),
  axis.text.x = element_blank(),
  axis.ticks.x = element_blank(),
  panel.grid.minor.y = element_blank(),
  panel.grid.major.y = element_blank(),
  legend.text = element_text(family="Lato"),
  legend.title=element_blank(),
  legend.direction = "vertical",
  legend.position = c(.85, .975)) +
  scale_y_continuous(expand = c(0,0),
                     limits = c(0,60)) +
  guides(fill = guide_legend(reverse=TRUE)) +
  scale_fill_manual(values = c("#fdbf11", "#1696d2")) +
  scale_x_discrete(name ="Labels", c("Not in labor force", "Unemployed", "Employed", " ","Not in metropolitan area", "In metropolitan area", " ","Above 400% FPL", "251%–400% FPL", "139%–250% FPL", "0%–138% FPL", " ", "Single, childless", "Married, childless", "Single mother", "Married mother", " ", "Other/multiple races", "Hispanic", "Black", "White", " ", "Ages 35–44", "Ages 26–34", "Ages 19–25", "Ages 15–18", " ", "All women (ages 15–44)"))+
  coord_flip()
ggsave("state_plot.pdf", plot = state_plot, path = "Plots", scale = 1, width =5, height = 6.5, units = "in", device = cairo_pdf)
```

\urbnlogo{}
\urbntitle{Insurance Coverage among Women of Reproductive Age} 
\vspace{-0.05in}
\urbntitle{in `r state_table$statename`}
The Affordable Care Act established several provisions aimed at reducing the uninsurance rate, and the number of uninsured women of reproductive age (15–44) in the United States fell from 12.7 million in 2013 to 7.5 million in 2017.

In `r state_table$statename`, `r statement1`, the uninsurance rate fell from `r format(round(state_table$ui_mn13, 1),nsmall=1)` percent in 2013 to `r format(round(state_table$ui_mn, 1),nsmall=1)` percent in 2017. Despite these gains, approximately `r statement7` `r statement8` women of reproductive age remained uninsured in 2017.

\vspace{-0.4in}
\urbnheadingone{Uninsurance Rate among Subgroups of Women in `r state_table$statename` and the US, 2017}
\columnratio{0.45}
\begin{paracol}{2}
Uninsured women are vulnerable to well-documented access, affordability, and health problems associated with lacking insurance coverage, including potentially limited access to family planning and other reproductive health services.
\begin{urbnbullets} 
\item Women of reproductive age in `r state_table$statename` had a `r statement2` uninsurance rate (`r format(round(state_table$ui_mn, 1),nsmall=1)` percent) `r statement3` similar women nationwide (11.7 percent) in 2017 (figure 1).
\vspace{0.1in}
\item Within `r state_table$statename`,`r statement4` `r statement9` for all women of reproductive age `r statement10` 
``` {r results = "asis", eval = is_large} 
cat("\\vspace{0.1in}
    \\item Reducing the uninsurance rate among women of reproductive age in ", params$parameter1, " may require ", statement5)
```
\end{urbnbullets}
``` {r results = "asis", eval = is_small} 
cat("\\urbnheadingone{Looking Ahead} 
    \\newline Reducing the uninsurance rate among women of reproductive age in ", params$parameter1, " may require ", statement5, " It will also be critical to track women’s ability to access the general and reproductive health services they need, including monitoring the availability and capacity of community health centers and Title X clinics. \\newline \\scriptsize{This fact sheet was prepared by Stacey McMorrow, Emily M. Johnston, and Tyler W. Thomas, with support from Leah Durbak and Michelle Menezes, and funded by the William and Flora Hewlett Foundation. For more information, download the technical appendix at \\href{https://urbn.is/30vKsI0}{https://urbn.is/30vKsI0}. The views expressed are those of the authors and should not be attributed to the Urban Institute, its trustees, or its funders. Further information on Urban’s funding principles is available at \\href{urban.org/fundingprinciples}{urban.org/fundingprinciples}. Copyright © July 2019. Urban Institute. Permission is granted for reproduction of this file, with attribution.}")
```
\switchcolumn
\urbnfigurenumber{1}
\urbnfiguretitle{Uninsurance Rate among Subgroups of Women of Reproductive Age in `r state_table$statename` and the US, 2017}
\includegraphics[width=3.7in]{Plots/state_plot.pdf}
\urbnsource{Source:} {Urban Institute analysis of 2017 American Community Survey.}
\urbnnote{Notes:} {FPL is federal poverty level. White, black, and other/multiple race are non-Hispanic.`r statement6` Differences reported in text are significant at p < 0.05.}
\end{paracol}
\contactinfo{}

``` {r results = "asis", eval = is_large}
cat("\\newpage
\\raggedright
\\urbnfigurenumber{2}
\\urbnfiguretitle{Potential Eligibility for Financial Assistance Obtaining Coverage among Uninsured Women of Reproductive Age in ", params$parameter1, ", 2017}", sep="")
```

```{r, fig.width=7.5, fig.height=2.25, fig.fullwidth=TRUE, eval = is_large}
par(lwd = 1)
datdict <- read.csv(file="Tables/State Uninsurance WRA Data Dictionary.csv", header=TRUE, sep=",")
colnames(datdict)[1]<-"Variable.Name"
row.names(datdict)<-datdict$Variable.Name
pie_labels<-datdict[c("hier_pov_none_share", "hier_noncit400pl_share","hier_mktplce_share","hier_medicaid_share","hier_fpw_share"),]["Label.for.Figures"]
pie_labels$var<-row.names(pie_labels)
pie_labs<-pie_labels %>% mutate(lab_for_figs = as.character(Label.for.Figures)) %>% select(var, lab_for_figs)
 #to force the same color for payment type in all figures
type<-factor(levels=c("hier_pov_none_share", "hier_noncit400pl_share","hier_mktplce_share","hier_medicaid_share","hier_fpw_share"))
myColors <- c("#fdbf11","#1696d2","#000000","#ec008b","#d2d2d2")
names(myColors) <- levels(type)
t<-state_table %>% 
  select(hier_pov_none_share, hier_noncit400pl_share,hier_mktplce_share,hier_medicaid_share,hier_fpw_share) %>% 
  gather(key="var",value="value") %>% 
  left_join(pie_labs) %>% 
  filter(value>0)
t$lab_for_figs_brks<-sapply(strwrap(t$lab_for_figs, 35, simplify=FALSE), paste, collapse="\n" )

t2<-t %>% 
  mutate(labels_w_num = paste(lab_for_figs_brks, " \n",format(round(value, 1),nsmall=1), "%", sep =""), 
         value = as.numeric(value),
         label_position = cumsum(value),
         end = 2 * pi * cumsum(value)/sum(value),
         start = lag(end, default = 0),
         middle = 0.5 * (start + end),
         nudgex = ifelse(length(value)==3 & end > pi*1.3 & end-start > 2.5 , -4, #nudge positions to left or right of pie depending on number (length(value)) and size (end-start) of slices
                  ifelse(length(value)==3 & end < pi*1.3, 3.5,
                  ifelse(length(value)==3 & end > pi*1.3, -3.5,
                  ifelse(length(value)==4 & end-start > 0.8 & start < pi*0.90 & end < pi*1.4, 3.5,
                  ifelse(length(value)==4 & end-start < 0.8 & start > pi*0.90 & end < pi*1.4, -3.5,
                  ifelse(length(value)==4 & end > pi*1.4, -3.5,
                  ifelse(length(value)==4 & end < pi, 3.5,-3.5))))))),
         nudgey = ifelse(middle < pi/2 | middle > pi*1.1, -0.25, 0.25),
         hjust = ifelse(middle > pi, 1, 0),
         vjust = ifelse(middle < pi/2 | middle > 3 * pi/2, 0, 1))
par(mar=rep(0,4))
 ggplot(t2) + 
  geom_arc_bar(aes(x0 = 0, y0 = 0, r0 = 0, r = 1,
                   start = start, end = end, fill = var),color = "white") +
  geom_text_repel(mapping = aes(x = 1.05 * sin(middle),
                y = 1.05 * cos(middle),
                label = labels_w_num,
                hjust = hjust,
                vjust = vjust),
  size = 2.6,
  nudge_x = t2$nudgex,
  nudge_y = t2$nudgey,
  segment.color = "black", 
  segment.size=0.35) +
  scale_fill_manual(values=myColors) +
  coord_fixed() +
  scale_x_continuous(limits = c(-4,4),  # Adjust so labels are not cut off
                     name = "", breaks = NULL, labels = NULL) +
  scale_y_continuous(limits = c(-1.25,1.25),      # Adjust so labels are not cut off
                     name = "", breaks = NULL, labels = NULL)+
  remove_ticks() +
  remove_axis() + 
  theme(legend.position = "none") 
```

``` {r results = "asis", eval = is_large}
cat(
"\\vspace{-0.2in}
\\urbnsource{Source:}{Urban Institute analysis of 2017 American Community Survey.} 
\\urbnnote{Note:}{CHIP is the Children's Health Insurance Program. ", pie_caption1, pie_caption2, "}
\\vspace{-0.6cm}") 
```

``` {r results = "asis", eval = is_large}
cat("Some women may remain uninsured because they lack an affordable coverage option, but others may not enroll in an affordable Medicaid, Children’s Health Insurance Program, or Marketplace plan because of a lack of awareness of their eligibility, administrative burdens, or concerns about enrolling in a public program. 

Among approximately ", statement7, " uninsured women of reproductive age in ", params$parameter1, " in 2017 (figure 2),")
```

``` {r results = "asis", eval = is_large}
cat("\\begin{urbnbullets}
\\item about ", format(round(state_table$hier_medicaid_share,1),nsmall=1), " percent were likely eligible for comprehensive Medicaid or Children’s Health Insurance Program coverage based on their income;
\\end{urbnbullets}
\\vspace{-0.3cm}")
```
```{r results = "asis", eval = is_large & (state_table$hier_100_138_share==0) & (state_table$expansion==0) & (state_table$statename!="Wisconsin")}
cat("\\begin{urbnbullets}
\\item another ", format(round(state_table$hier_mktplce_share,1),nsmall=1), " percent were likely eligible for assistance with premiums for Marketplace coverage based on their income;
\\end{urbnbullets}
\\vspace{-0.3cm}")
```
```{r results = "asis", eval = is_large & (state_table$hier_100_138_share>0) & (state_table$expansion==0) & (state_table$statename!="Wisconsin")}
cat("\\begin{urbnbullets}
\\item another ", format(round(state_table$hier_mktplce_share,1),nsmall=1), " percent were likely eligible for assistance with premiums for Marketplace coverage based on their income, including ", format(round(state_table$hier_100_138_share,1),nsmall=1), " percent with incomes between 100 and 138 percent of the FPL;
\\end{urbnbullets}
\\vspace{-0.3cm}")
```

```{r results = "asis", eval = is_large & (state_table$hier_100_138_share==0) & (state_table$expansion==1 | state_table$statename=="Wisconsin")}
cat("\\begin{urbnbullets}
\\item another ", format(round(state_table$hier_mktplce_share,1),nsmall=1), " percent were likely eligible for assistance with premiums for Marketplace coverage based on their income; and
\\end{urbnbullets}
\\vspace{-0.3cm}")
```
```{r results = "asis", eval = is_large & (state_table$hier_100_138_share>0) & (state_table$expansion==1 | state_table$statename=="Wisconsin")}
cat("\\begin{urbnbullets}
\\item another ", format(round(state_table$hier_mktplce_share,1),nsmall=1), " percent were likely eligible for assistance with premiums for Marketplace coverage based on their income, including ", format(round(state_table$hier_100_138_share,1),nsmall=1), " percent with incomes between 100 and 138 percent of the FPL; and
\\end{urbnbullets}
\\vspace{-0.3cm}")
```

```{r results = "asis", eval = is_large & (state_table$hier_fpw_share>0)}
cat("\\begin{urbnbullets}
\\item about ", format(round(state_table$hier_fpw_share,1),nsmall=1), " percent had incomes below the FPL and were likely eligible for coverage for family planning services only; and 
\\end{urbnbullets}
\\vspace{-0.3cm}")
```
``` {r results = "asis", eval = is_large & (state_table$hier_pov_none_share>0)}
cat("\\begin{urbnbullets}
\\item about ", format(round(state_table$hier_pov_none_share,1),nsmall=1), " percent were women with incomes below the FPL who were likely ineligible for assistance obtaining comprehensive Medicaid, Children’s Health Insurance Program, or Marketplace coverage; and
\\end{urbnbullets}
\\vspace{-0.3cm}")
```
``` {r results = "asis", eval = is_large}
cat("\\begin{urbnbullets}
\\item ", paste("about ", format(round(state_table$hier_noncit400pl_share,1),nsmall=1), " percent were likely ineligible for assistance obtaining comprehensive health insurance, including noncitizens (",format(round(state_table$hier_noncit_share,1),nsmall=1), " percent), and women with incomes above 400 percent of the FPL (",format(round(state_table$hier_400pl_share,1),nsmall=1), " percent).",sep=""), " 
\\end{urbnbullets}
\\vspace{-0.8cm}")
```
``` {r results = "asis", eval = is_large}
cat("\\urbnheadingone{Looking Ahead} ",
"\\newline ", lookingahead, outreach, "\\newline", authors, "\\urbnboilerplate{the William and Flora Hewlett Foundation}{July}{2019}")
```


